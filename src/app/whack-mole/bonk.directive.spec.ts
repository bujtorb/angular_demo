import { BonkDirective } from './bonk.directive';

describe('BonkDirective', () => {
  it('should create an instance', () => {
    const directive = new BonkDirective();
    expect(directive).toBeTruthy();
  });
});
