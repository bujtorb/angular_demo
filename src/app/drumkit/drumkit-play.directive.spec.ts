import { DrumkitPlayDirective } from './drumkit-play.directive';

describe('DrumkitPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new DrumkitPlayDirective();
    expect(directive).toBeTruthy();
  });
});
