import { VariablesControlDirective } from './variables-control.directive';

describe('VariablesControlDirective', () => {
  it('should create an instance', () => {
    const directive = new VariablesControlDirective();
    expect(directive).toBeTruthy();
  });
});
